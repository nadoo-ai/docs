---
title: 'API Reference'
description: 'Complete API documentation for Nadoo AI platform'
sidebarTitle: 'Overview'
'og:title': 'Nadoo AI API Reference'
icon: 'code'
---

## API Documentation

Comprehensive API reference for all Nadoo AI platform components.

## Available APIs

<CardGroup cols={2}>
  <Card
    title="Flow Core APIs"
    icon="cube"
    href="/api/flow-core/base"
  >
    **Open Source Python Framework**

    Workflow orchestration and AI agent development
  </Card>
  <Card
    title="Plugin SDK APIs"
    icon="puzzle-piece"
    href="/api/plugin-sdk/llm"
  >
    **Plugin Development**

    Build custom tools and integrations
  </Card>
  <Card
    title="Sandbox APIs"
    icon="shield"
    href="/api/sandbox/execute"
  >
    **Secure Code Execution**

    Multi-language code execution service
  </Card>
  <Card
    title="Builder APIs"
    icon="palette"
    href="/api/builder/rest-api"
  >
    **Coming Soon**

    Visual builder platform APIs
  </Card>
</CardGroup>

---

## Flow Core APIs

Python framework for building AI workflows and agents.

### Core Modules

| Module | Description | Status |
|--------|-------------|--------|
| [`base`](/api/flow-core/base) | Core types and workflow execution | ✅ Stable |
| [`chain`](/api/flow-core/chain) | Chaining API with pipe operator | ✅ Stable |
| [`batch`](/api/flow-core/batch) | Batch processing operations | ✅ Stable |
| [`callbacks`](/api/flow-core/callbacks) | Event handling and observability | ✅ Stable |

### Feature Modules

| Module | Description | Status |
|--------|-------------|--------|
| [`memory`](/api/flow-core/memory) | Chat history and session management | ✅ Stable |
| [`memory_db`](/api/flow-core/memory_db) | Database-backed persistent storage | ✅ Stable |
| [`streaming`](/api/flow-core/streaming) | Real-time event streaming | ✅ Stable |
| [`resilience`](/api/flow-core/resilience) | Retry and fallback patterns | ✅ Stable |
| [`caching`](/api/flow-core/caching) | Response caching mechanisms | ✅ Stable |
| [`rate_limiting`](/api/flow-core/rate_limiting) | API quota management | ✅ Stable |
| [`tools`](/api/flow-core/tools) | Tool registry and management | ✅ Stable |
| [`parsers`](/api/flow-core/parsers) | Output parsing and extraction | ✅ Stable |
| [`prompts`](/api/flow-core/prompts) | Prompt templates and formatting | ✅ Stable |
| [`backends`](/api/flow-core/backends) | Multi-backend support | ✅ Stable |

### Quick Example

```python
from nadoo_flow import ChainableNode, NodeResult

class MyNode(ChainableNode):
    async def execute(self, node_context, workflow_context):
        return NodeResult(success=True, output={"result": "data"})

# Chain nodes with pipe operator
workflow = MyNode() | ProcessNode() | OutputNode()
result = await workflow.run({"input": "data"})
```

---

## Plugin SDK APIs

SDK for developing custom plugins and tools.

### Available APIs

| API | Description | Documentation |
|-----|-------------|---------------|
| [`llm`](/api/plugin-sdk/llm) | Invoke LLM models | ✅ Available |
| [`knowledge`](/api/plugin-sdk/knowledge) | Search knowledge bases | ✅ Available |
| [`tools`](/api/plugin-sdk/tools) | Invoke other tools | ✅ Available |
| [`storage`](/api/plugin-sdk/storage) | Persistent storage | ✅ Available |

### Quick Example

```python
from nadoo_plugin import NadooPlugin, tool, parameter

class MyPlugin(NadooPlugin):
    @tool(name="process_text", description="Process text using AI")
    @parameter("text", type="string", required=True)
    def process_text(self, text: str) -> dict:
        # Use Nadoo's LLM
        response = self.api.llm.invoke(
            messages=[
                {"role": "user", "content": f"Process: {text}"}
            ]
        )

        return {"processed": response.content}
```

---

## Sandbox APIs

Secure code execution in 12+ programming languages.

### Endpoints

| Endpoint | Description | Documentation |
|----------|-------------|---------------|
| [`/execute`](/api/sandbox/execute) | Synchronous execution | ✅ Available |
| [`/async-execute`](/api/sandbox/async-execute) | Asynchronous execution | ✅ Available |
| [`/batch`](/api/sandbox/batch) | Batch execution | ✅ Available |

### Supported Languages

Python, JavaScript, TypeScript, Java, Go, Rust, C++, C#, Ruby, PHP, SQL, Bash

### Quick Example

```bash
curl -X POST http://localhost:8002/api/v1/execute \
  -H "X-API-Key: your-api-key" \
  -H "Content-Type: application/json" \
  -d '{
    "code": "print(\"Hello, World!\")",
    "language": "python"
  }'
```

---

## Builder APIs

<Note>
  **Coming Soon**

  Builder platform APIs for visual workflow creation and management.
</Note>

Planned APIs:
- REST API for workflow management
- Webhooks for event notifications
- SDK for programmatic access

---

## API Conventions

### Import Structure

All APIs follow consistent import patterns:

```python
# Flow Core
from nadoo_flow import ChainableNode, WorkflowExecutor

# Plugin SDK
from nadoo_plugin import NadooPlugin, tool, parameter

# For Sandbox, use REST API
```

### Async-First Design

Flow Core and Plugin SDK use async/await:

```python
# Async execution
result = await node.execute(node_context, workflow_context)
result = await workflow.run(input_data)

# Streaming
async for chunk in workflow.stream(input_data):
    process(chunk)
```

### Type Safety

All APIs use type hints and Pydantic models:

```python
from typing import Dict, Any
from pydantic import BaseModel

class Config(BaseModel):
    timeout: float = 30.0
    retries: int = 3
```

### Error Handling

Consistent error handling across all APIs:

```python
from nadoo_flow import NodeResult

try:
    result = await node.execute(context)
    return NodeResult(success=True, output=data)
except Exception as e:
    return NodeResult(success=False, error=str(e))
```

---

## Authentication

### Flow Core

No authentication required - local Python library

### Plugin SDK

Uses workspace API keys configured in Nadoo platform

### Sandbox

API key authentication via `X-API-Key` header:

```bash
curl -H "X-API-Key: your-api-key" ...
```

### Builder

OAuth 2.0 and API key authentication (coming soon)

---

## Getting Started

<CardGroup cols={2}>
  <Card title="Flow Core Quickstart" icon="rocket" href="/flow-core/quickstart">
    Build your first workflow
  </Card>
  <Card title="Plugin SDK Quickstart" icon="plug" href="/plugin-sdk/quickstart">
    Create your first plugin
  </Card>
  <Card title="Sandbox Quickstart" icon="play" href="/sandbox/quickstart">
    Execute code securely
  </Card>
  <Card title="Builder Guide" icon="wand-magic-sparkles" href="/builder/getting-started">
    Visual workflow editor
  </Card>
</CardGroup>

---

## Support

<CardGroup cols={3}>
  <Card title="GitHub" icon="github" href="https://github.com/nadoo-ai">
    View source code and contribute
  </Card>
  <Card title="Discord" icon="discord" href="https://discord.gg/9gCsxSn6">
    Join our community
  </Card>
  <Card title="Documentation" icon="book" href="/introduction">
    Read the full documentation
  </Card>
</CardGroup>
